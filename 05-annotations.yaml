apiVersion: v1
kind: Pod
metadata:
  name: annotations
  annotations:
    imageregistry: "https://hub.docker.com/"
    #buildURL: "https://jenkins.com"
spec:
  containers:
  - name: nginx
    image: nginx     